const actionablePages = [
  'user/smartlife_add_note',
  'user/smartlife_add_income',
  'user/smartlife_add_activity',
  'user/smartlife_add_task',
  'user/smartlife_add_appointment',
  'user/smartlife_save_activity',
  'user/smartlife_save_task',
  'user/smartlife_save_appointment',
  'user/smartlife_scan_result',
  'user/smartlife_receipt_result',
];

function pageKeyFromHtml(html: string) {
  return html.match(/const pageKey = ["']([^"']+)["']/)?.[1] ?? '';
}

function disablePrototypeNavigation(html: string, pageKey: string) {
  if (!actionablePages.includes(pageKey)) return html;
  if (pageKey.includes('_add_')) {
    return html
      .replace(/(<a class="save-mini" href=")[^"]+/, '$1#smartlife-save')
      .replace(/(<a class="primary" href=")[^"]+/, '$1#smartlife-save')
      .replace(/(<a class="save-button" href=")[^"]+/, '$1#smartlife-save');
  }
  if (pageKey === 'user/smartlife_scan_result') {
    return html.replace(/(<a class="primary" href=")[^"]+/, '$1#smartlife-save');
  }
  return html.replace(/(<a class="save-button" href=")[^"]+/, '$1#smartlife-save');
}

function withAdminFallback(html: string, pageKey: string) {
  if (!pageKey.startsWith('admin/')) return html;

  const labels: Record<string, {subtitle: string; title: string}> = {
    admin_dashboard: {title: 'Dashboard ภาพรวมระบบ', subtitle: 'ข้อมูลจริงจาก SmartLife'},
    admin_users: {title: 'จัดการผู้ใช้', subtitle: 'บัญชีและสถานะการใช้งาน'},
    admin_categories: {title: 'จัดการหมวดหมู่', subtitle: 'โน้ต การเงิน และกิจกรรม'},
    admin_ai_knowledge: {title: 'AI Knowledge Monitor', subtitle: 'ข้อมูลที่ AI ใช้ประกอบคำแนะนำ'},
    admin_ocr_logs: {title: 'OCR / Import Log', subtitle: 'ประวัติการสแกนและนำเข้าข้อมูล'},
    admin_announcements: {title: 'ประกาศ', subtitle: 'ข่าวสารถึงผู้ใช้ SmartLife'},
    admin_feedback: {title: 'Feedback Center', subtitle: 'ความคิดเห็นจากผู้ใช้'},
    admin_system_health: {title: 'System Health', subtitle: 'สถานะการให้บริการ'},
  };
  const page = labels[pageKey.slice('admin/'.length)] ?? labels.admin_dashboard;
  const fallback = `<div id="admin-app"><div class="admin-shell"><aside class="sidebar"><a class="brand" href="admin_dashboard.html"><span class="brand-mark">SL</span><span><strong>SmartLife Admin</strong><small>Operations Console</small></span></a></aside><main class="main"><section class="page-head"><div><h1>${page.title}</h1><p>${page.subtitle}</p></div></section><section class="metric-grid"><article class="metric"><span class="metric-icon">&#128101;</span><span>ผู้ใช้ทั้งหมด</span><strong>0</strong><small>ข้อมูลปัจจุบัน</small></article><article class="metric"><span class="metric-icon">&#128197;</span><span>ตารางเรียนที่เพิ่ม</span><strong>0</strong><small>ข้อมูลปัจจุบัน</small></article><article class="metric"><span class="metric-icon">&#128221;</span><span>โน้ตที่บันทึก</span><strong>0</strong><small>ข้อมูลปัจจุบัน</small></article><article class="metric"><span class="metric-icon">&#3647;</span><span>รายการการเงิน</span><strong>0</strong><small>ข้อมูลปัจจุบัน</small></article><article class="metric"><span class="metric-icon">AI</span><span>การใช้งาน AI</span><strong>0</strong><small>ข้อมูลปัจจุบัน</small></article></section><section class="panel"><div class="panel-head"><h2>กำลังเชื่อมต่อข้อมูล Admin</h2><span>Firebase</span></div><div class="panel-body"><p style="margin:0;color:var(--muted);font-size:12px">ข้อมูลจะแสดงตามสิทธิ์ผู้ดูแลระบบ</p></div></section></main><nav aria-label="เมนูหลักแอดมิน" style="height:76px;display:grid;grid-template-columns:repeat(5,1fr);align-items:center;padding:7px 14px 8px;border-top:1px solid rgba(40,51,31,.1);border-radius:0 0 25px 25px;background:#fff;box-shadow:0 -10px 22px rgba(40,51,31,.06)"><a href="admin_dashboard.html" style="display:flex;flex-direction:column;align-items:center;gap:4px;color:#415d36;font-size:9px;font-weight:800;text-decoration:none"><b style="font-size:20px;line-height:1">&#8862;</b><span>Dashboard</span></a><a href="admin_users.html" style="display:flex;flex-direction:column;align-items:center;gap:4px;color:#9ea59b;font-size:9px;font-weight:700;text-decoration:none"><b style="font-size:20px;line-height:1">&#9787;</b><span>ผู้ใช้</span></a><a href="admin_more.html" aria-label="เมนูเพิ่มเติม" style="display:flex;justify-content:flex-start;align-items:center;text-decoration:none"><b style="width:56px;height:56px;margin-top:-18px;display:grid;place-items:center;border:5px solid #fff;border-radius:50%;background:linear-gradient(180deg,#66865e,#415d36);box-shadow:0 12px 24px rgba(44,52,27,.28);color:#fff;font-size:28px;line-height:1">&#8862;</b></a><a href="admin_ai_knowledge.html" style="display:flex;flex-direction:column;align-items:center;gap:4px;color:#9ea59b;font-size:9px;font-weight:700;text-decoration:none"><b style="font-size:19px;line-height:1">&#9881;</b><span>AI</span></a><a href="admin_system_health.html" style="display:flex;flex-direction:column;align-items:center;gap:4px;color:#9ea59b;font-size:9px;font-weight:700;text-decoration:none"><b style="font-size:19px;line-height:1">&#8764;</b><span>ระบบ</span></a></nav></div></div>`;
  return html.replace('<div id="admin-app"></div>', fallback);
}

export function withLegacyDataBridge(html: string) {
  const pageKey = pageKeyFromHtml(html);
  if (!pageKey || pageKey.startsWith('login/')) return html;
  const prepared = withAdminFallback(disablePrototypeNavigation(html, pageKey), pageKey);
  const script = `<script data-smartlife-firestore>(()=>{
const pageKey=${JSON.stringify(pageKey)};const send=(payload)=>window.__smartlifeSend?.(payload);
const esc=(value)=>String(value??'').replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const date=(value,options={})=>value?new Intl.DateTimeFormat('th-TH',{dateStyle:'medium',timeZone:'Asia/Bangkok',...options}).format(new Date(value)):'-';
const time=(value)=>value?new Intl.DateTimeFormat('th-TH',{hour:'2-digit',minute:'2-digit',hour12:false,timeZone:'Asia/Bangkok'}).format(new Date(value)):'-';
const money=(value)=>'฿'+Number(value||0).toLocaleString('th-TH',{maximumFractionDigits:2});
const all=(selector)=>Array.from(document.querySelectorAll(selector));
const text=(selector,value)=>{const el=document.querySelector(selector);if(el)el.textContent=String(value??'')};
const updateClock=()=>text('.status > span:first-child',new Intl.DateTimeFormat('th-TH',{hour:'2-digit',minute:'2-digit',hour12:false,timeZone:'Asia/Bangkok'}).format(new Date()));updateClock();setInterval(updateClock,30000);
const renderFinance=(data)=>{const items=data.transactions||[];const income=items.filter(x=>x.type==='income').reduce((s,x)=>s+Number(x.amount||0),0);const expense=items.filter(x=>x.type==='expense').reduce((s,x)=>s+Number(x.amount||0),0);const period=pageKey.includes('_month')?new Intl.DateTimeFormat('th-TH',{month:'long',year:'numeric',timeZone:'Asia/Bangkok'}).format(new Date()):pageKey.includes('_week')?'สัปดาห์นี้':'วันนี้ '+date(new Date());text('.title-copy p',period);text('.balance-total strong',money(income-expense));const cards=all('.summary-card');cards.forEach(card=>{const isIncome=card.classList.contains('income');const amount=card.querySelector('.amount,strong:last-child');if(amount)amount.textContent=money(isIncome?income:expense)});const list=document.querySelector('.transactions');if(list)list.innerHTML=items.length?items.map(x=>'<article class="transaction"><span class="tx-icon">'+(x.type==='income'?'+':'−')+'</span><span><strong>'+esc(x.merchant||x.category)+'</strong><small>'+esc(x.category)+' · '+date(x.occurredAt)+'</small></span><strong class="amount '+(x.type==='income'?'income':'')+'">'+(x.type==='income'?'+':'−')+money(x.amount)+'</strong></article>').join(''):'<p style="padding:18px;text-align:center;color:#81887d">ยังไม่มีรายการในช่วงเวลานี้</p>'};
const renderNotes=(data)=>{const items=data.notes||[];const list=document.querySelector('.note-list');if(list)list.innerHTML=items.length?items.map(x=>'<article class="note-card"><span class="note-icon">✓</span><span><strong>'+esc(x.title)+'</strong><small>'+date(x.updatedAt)+' · '+esc(x.category)+'</small><p>'+esc(String(x.content||'').slice(0,100))+'</p></span></article>').join(''):'<p style="padding:18px;text-align:center;color:#81887d">ยังไม่มีโน้ต</p>';const stats=all('.stats .stat strong');if(stats[0])stats[0].textContent=String(items.length)};
const renderCalendar=(data)=>{const now=new Date();text('.month-nav strong',new Intl.DateTimeFormat('th-TH',{month:'long',year:'numeric',timeZone:'Asia/Bangkok'}).format(now));text('.day-hero h2',new Intl.DateTimeFormat('th-TH',{weekday:'long',timeZone:'Asia/Bangkok'}).format(now));text('.day-hero p',date(now));const items=[...(data.schedules||[]),...(data.activities||[])].sort((a,b)=>new Date(a.startAt)-new Date(b.startAt));const timeline=document.querySelector('.timeline');if(timeline)timeline.innerHTML=items.length?items.map(x=>'<article class="timeline-item"><div class="timeline-time">'+time(x.startAt)+' - '+time(x.endAt)+'</div><div class="timeline-card" style="border-left-color:'+esc(x.color||'#6F8F6D')+'"><h2>'+esc(x.title)+'</h2><p>'+esc(x.courseCode||x.type||'')+(x.location?' · '+esc(x.location):'')+'</p></div></article>').join(''):'<p style="padding:20px;text-align:center;color:#81887d">ยังไม่มีตารางในวันนี้</p>';const agenda=document.querySelector('.month-agenda');if(agenda)agenda.innerHTML=items.length?items.map(x=>'<article class="agenda-card"><div class="agenda-date">'+new Intl.DateTimeFormat('en',{day:'numeric',timeZone:'Asia/Bangkok'}).format(new Date(x.startAt))+'</div><div><h2>'+esc(x.title)+'</h2><p>'+date(x.startAt)+' · '+time(x.startAt)+(x.location?' · '+esc(x.location):'')+'</p></div></article>').join(''):'<p style="padding:20px;text-align:center;color:#81887d">ยังไม่มีรายการเดือนนี้</p>';const days=all('.week-day');if(days.length){const labels=Array.from({length:7},(_,index)=>new Date(new Date(data.from||now).getTime()+index*86400000));days.forEach((day,index)=>{const current=labels[index];const textNode=day.querySelector('.week-date strong');if(textNode)textNode.textContent=new Intl.DateTimeFormat('en',{day:'numeric',timeZone:'Asia/Bangkok'}).format(current);const key=new Intl.DateTimeFormat('en-CA',{year:'numeric',month:'2-digit',day:'2-digit',timeZone:'Asia/Bangkok'}).format(current);const box=day.querySelector('.week-events');const found=items.filter(x=>new Intl.DateTimeFormat('en-CA',{year:'numeric',month:'2-digit',day:'2-digit',timeZone:'Asia/Bangkok'}).format(new Date(x.startAt))===key);if(box)box.innerHTML=found.map(x=>'<div class="week-event"><strong>'+esc(x.title)+'</strong><span>'+time(x.startAt)+' - '+time(x.endAt)+' · '+esc(x.location||'')+'</span></div>').join('')})}};
const renderNotifications=(data)=>{const items=data.notifications||[];const lists=all('.notice-list');lists.forEach((list,index)=>{list.innerHTML=index===0?(items.length?items.map(x=>'<article class="notice-card '+esc(x.kind)+'" data-id="'+esc(x.id)+'"><span class="notice-icon">!</span><span class="notice-body"><strong>'+esc(x.title)+'</strong><p>'+esc(x.message)+'</p><small class="notice-meta">'+date(x.createdAt)+' · '+time(x.createdAt)+'</small></span></article>').join(''):'<p style="padding:18px;text-align:center;color:#81887d">ไม่มีการแจ้งเตือน</p>'):''});text('.bell-badge',items.filter(x=>!x.read).length)};
const renderDashboard=(data)=>{const schedule=data.schedules||[];const activity=data.activities||[];const transactions=data.transactions||[];text('.name',data.profile?.displayName||'ผู้ใช้');const stats=all('.stats .stat strong');if(stats[0])stats[0].textContent=String(schedule.length);if(stats[1])stats[1].textContent=String(activity.filter(x=>x.status!=='completed').length);if(stats[2])stats[2].textContent=money(transactions.filter(x=>x.type==='expense').reduce((n,x)=>n+Number(x.amount||0),0));const scheduleBox=document.querySelector('.schedule');if(scheduleBox)scheduleBox.innerHTML=schedule.length?schedule.slice(0,3).map(x=>'<div class="course"><span class="time">'+time(x.startAt)+'</span><span><strong>'+esc(x.title)+'</strong><small>'+esc(x.location||x.courseCode||'')+'</small></span></div>').join(''):'<p style="padding:12px;text-align:center;color:#81887d">วันนี้ยังไม่มีคลาส</p>';const taskList=document.querySelector('.task-list');if(taskList)taskList.innerHTML=activity.length?activity.slice(0,4).map(x=>'<div class="task"><span class="task-dot"></span><span><strong>'+esc(x.title)+'</strong><small>'+time(x.startAt)+' · '+esc(x.type)+'</small></span></div>').join(''):'<p style="padding:12px;text-align:center;color:#81887d">ยังไม่มีงานวันนี้</p>';const priority=document.querySelector('.priority-list');const upcoming=[...schedule,...activity].sort((a,b)=>new Date(a.startAt)-new Date(b.startAt));if(priority)priority.innerHTML=upcoming.slice(0,3).map((x,i)=>'<article class="priority-item"><span class="priority-rank">'+(i+1)+'</span><span><strong>'+esc(x.title)+'</strong><small>'+time(x.startAt)+' · '+esc(x.location||x.type||'')+'</small></span></article>').join('')};
const renderAi=(data)=>{const items=data.recommendations||[];const list=document.querySelector('.priority-list,.recommendation-list');if(list&&items.length)list.innerHTML=items.map(x=>'<article class="priority-item"><span class="priority-rank">AI</span><span><strong>'+esc(x.title)+'</strong><small>'+esc(x.explanation)+'</small></span></article>').join('')};
const renderProfile=(data)=>{const p=data.profile||{};text('.profile-card h2',p.displayName||'ผู้ใช้ SmartLife');text('.profile-card p',p.email||'');const avatar=document.querySelector('.profile-card .avatar');if(avatar)avatar.textContent=String(p.displayName||'SL').slice(0,2).toUpperCase();const stats=all('.stats .stat strong');const c=data.counts||{};[c.schedules,c.notes,c.transactions].forEach((v,i)=>{if(stats[i])stats[i].textContent=String(v||0)})};
const renderAdmin=(data)=>{if(data.counts){const values=all('.metric strong');const c=data.counts;[c.users,c.schedules,c.notes,c.transactions,c.aiRecommendations].forEach((v,i)=>{if(values[i])values[i].textContent=Number(v||0).toLocaleString('th-TH')})}if(data.users){const body=document.querySelector('tbody');if(body)body.innerHTML=data.users.map(u=>'<tr><td><span class="person"><span class="person-dot">'+esc(String(u.displayName||'U').slice(0,2))+'</span><span><strong>'+esc(u.displayName||'ผู้ใช้')+'</strong><small>'+esc(u.email||'')+'</small></span></span></td><td><span class="pill '+(u.disabled?'danger':'')+'">'+(u.disabled?'ระงับ':'ใช้งานปกติ')+'</span></td><td>'+date(u.createdAt)+'</td><td>'+(u.lastSignInAt?date(u.lastSignInAt):'-')+'</td><td><button class="text-button" data-user-toggle="'+esc(u.uid)+'" data-disabled="'+(!u.disabled)+'">'+(u.disabled?'เปิดใช้งาน':'ระงับ')+'</button><button class="text-button" data-user-reset="'+esc(u.email)+'">รีเซ็ตรหัสผ่าน</button></td></tr>').join('')}if(data.scanLogs){const body=document.querySelector('tbody');if(body)body.innerHTML=data.scanLogs.map(x=>'<tr><td>'+date(x.createdAt)+' '+time(x.createdAt)+'</td><td>'+esc(x.kind)+'</td><td>'+esc(String(x.extractedText||'').slice(0,80))+'</td><td>'+esc(x.errorMessage||'OCR')+'</td><td><span class="pill">'+esc(x.status)+'</span></td></tr>').join('')}if(data.feedback){const list=document.querySelector('.feedback-list');if(list)list.innerHTML=data.feedback.map(x=>'<div class="feedback-row"><span class="person-dot">FB</span><span><strong>'+esc(x.message)+'</strong><small>'+esc(x.type)+' · '+date(x.createdAt)+'</small></span><span class="pill accent">'+esc(x.status)+'</span></div>').join('')}if(data.categories){const groups=all('.category-tags');['note','expense','activity'].forEach((domain,i)=>{if(groups[i])groups[i].innerHTML=data.categories.filter(x=>x.domain===domain).map(x=>'<span class="category-tag">'+esc(x.labelTh)+'</span>').join('')})}if(data.announcements){const list=document.querySelector('.activity-list');if(list)list.innerHTML=data.announcements.map(x=>'<div class="activity-row"><span class="activity-dot"></span><span><strong>'+esc(x.title)+'</strong><small>'+date(x.createdAt)+' · '+esc(x.kind)+'</small></span></div>').join('')}if(data.systemStatus){const list=document.querySelector('.health-list');if(list)list.innerHTML=data.systemStatus.map(x=>'<div class="health-row"><span class="health-dot"></span><span><strong>'+esc(x.name)+'</strong><small>'+esc(x.detail)+' · '+Number(x.latencyMs||0)+' ms</small></span><span class="pill">'+esc(x.status)+'</span></div>').join('')}if(data.recommendations){const body=document.querySelector('tbody');if(body)body.innerHTML=data.recommendations.map(x=>'<tr><td>'+esc(x.title)+'</td><td>'+esc((x.contextSources||[]).join(', '))+'</td><td>'+esc(x.kind)+'</td><td><span class="pill">'+esc(x.status)+'</span></td></tr>').join('')}};
const renderAdminDashboard=(data)=>{if(!pageKey.includes('dashboard'))return;const activity=document.querySelector('.activity-list');if(activity&&data.scanLogs)activity.innerHTML=data.scanLogs.slice(0,4).map(x=>'<div class="activity-row"><span class="activity-dot"></span><span><strong>'+esc(x.kind==='receipt'?'OCR receipt imported':'OCR schedule imported')+'</strong><small>'+esc(String(x.extractedText||'').slice(0,72))+'</small></span><time>'+time(x.createdAt)+'</time></div>').join('');const health=document.querySelector('.health-list');if(health&&data.systemStatus)health.innerHTML=data.systemStatus.map(x=>'<div class="health-row"><span class="health-dot"></span><span><strong>'+esc(x.name)+'</strong><small>'+esc(x.detail)+' - '+Number(x.latencyMs||0)+' ms</small></span><span class="pill">'+esc(x.status)+'</span></div>').join('')};
const render=(data)=>{if(pageKey.startsWith('admin/')){renderAdmin(data);return renderAdminDashboard(data)}if(pageKey==='user/index')renderDashboard(data);if(pageKey.includes('calendar'))renderCalendar(data);if(pageKey.includes('finance'))renderFinance(data);if(pageKey.includes('notes'))renderNotes(data);if(pageKey.includes('notifications'))renderNotifications(data);if(pageKey.includes('profile'))renderProfile(data);if(pageKey.includes('ai_'))renderAi(data)};
const action=(name,payload,target)=>{window.__smartlifePendingTarget=target||'';send({type:'smartlife:data-action',pageKey,action:name,payload})};
const setupForms=()=>{if(pageKey==='user/smartlife_add_note'){all('.tag-chip').forEach(chip=>chip.addEventListener('click',()=>{all('.tag-chip').forEach(x=>x.classList.remove('active'));chip.classList.add('active')}));const save=all('.save-mini,.primary');save.forEach(el=>el.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();const active=document.querySelector('.tag-chip.active')?.textContent?.trim();const category=active==='งาน'?'work':active==='ไอเดีย'?'idea':'study';action('create-note',{title:document.querySelector('#note-title')?.value,content:document.querySelector('#note-body')?.value,category,color:'#6F8F6D'},'smartlife_notes.html')},true))}if(pageKey==='user/smartlife_add_income'){all('.source').forEach(source=>source.addEventListener('click',e=>{e.preventDefault();all('.source').forEach(x=>x.classList.remove('active'));source.classList.add('active')}));const input=document.querySelector('#income-date');if(input){input.type='datetime-local';const now=new Date(Date.now()-new Date().getTimezoneOffset()*60000);input.value=now.toISOString().slice(0,16)}all('.save-mini,.primary').forEach(el=>el.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();action('create-transaction',{type:'income',amount:document.querySelector('#income-amount')?.value,category:document.querySelector('.source.active strong')?.textContent||'รายรับ',merchant:document.querySelector('#income-title')?.value,note:document.querySelector('#income-note')?.value,occurredAt:new Date(document.querySelector('#income-date')?.value||Date.now()).toISOString()},'smartlife_finance_income.html')},true))}if(pageKey.includes('notifications'))document.addEventListener('click',e=>{const card=e.target.closest?.('.notice-card[data-id]');if(card)action('mark-notification-read',{id:card.dataset.id})});if(pageKey==='user/smartlife_profile'){document.querySelector('#feedback-form')?.addEventListener('submit',e=>{e.preventDefault();const label=document.querySelector('#feedback-type')?.value||'';const type=label.includes('AI')?'ai':label.includes('ตาราง')?'schedule-scan':label.includes('รายจ่าย')?'expense-category':'other';action('feedback',{type,message:document.querySelector('#feedback-message')?.value})})}if(pageKey.startsWith('admin/')&&pageKey.includes('announcements'))document.querySelector('[data-action="announce"]')?.addEventListener('click',()=>{const fields=all('.form-panel .field-input');action('create-announcement',{title:fields[0]?.value,message:fields[1]?.value})});if(pageKey.startsWith('admin/')&&pageKey.includes('categories')){const input=document.querySelector('.field-input');if(input&&!document.querySelector('#smartlife-category-domain'))input.insertAdjacentHTML('beforebegin','<select id="smartlife-category-domain" class="field-input"><option value="note">โน้ต</option><option value="expense">รายจ่าย</option><option value="activity">กิจกรรม</option></select>');document.querySelector('[data-action="create"]')?.addEventListener('click',()=>{const name=document.querySelectorAll('.field-input')[1]||document.querySelector('.field-input');action('create-category',{domain:document.querySelector('#smartlife-category-domain')?.value||'activity',labelTh:name?.value,labelEn:name?.value,color:'#6F8F6D'})})}if(pageKey.startsWith('admin/')&&pageKey.includes('users'))document.addEventListener('click',e=>{const toggle=e.target.closest?.('[data-user-toggle]');const reset=e.target.closest?.('[data-user-reset]');if(toggle)action('toggle-user',{uid:toggle.dataset.userToggle,disabled:toggle.dataset.disabled==='true'});if(reset)action('reset-user-password',{email:reset.dataset.userReset})});
if(pageKey==='user/smartlife_receipt_result')document.querySelector('.save-button')?.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();const result=window.__smartlifeOcrResult||{};const parsed=result.parsed||{};action('create-transaction',{type:'expense',amount:parsed.total,category:'อาหาร',merchant:parsed.merchant||'รายการจากใบเสร็จ',note:'บันทึกด้วย OCR',occurredAt:new Date().toISOString(),receiptPath:result.storagePath||''},'smartlife_finance_expense.html')},true);
if(pageKey==='user/smartlife_scan_result')document.querySelector('.primary')?.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();const result=window.__smartlifeOcrResult||{};const rows=Array.isArray(result.parsed?.entries)?result.parsed.entries:[];const today=new Date().toISOString().slice(0,10);const entries=rows.map((row,index)=>{const start=new Date(today+'T'+(row.time||String(9+index).padStart(2,'0')+':00')+':00');return{title:row.raw||'รายการจากตารางเรียน',courseCode:'',location:'',startAt:start.toISOString(),endAt:new Date(start.getTime()+60*60*1000).toISOString(),color:'#6F8F6D'}});action('create-schedules',{entries},'smartlife_calendar_day.html')},true);
if(pageKey.startsWith('user/smartlife_add_')&&['activity','task','appointment'].some(x=>pageKey.endsWith(x))){const type=pageKey.endsWith('task')?'task':pageKey.endsWith('appointment')?'appointment':'activity';const today=new Date().toISOString().slice(0,10);document.querySelectorAll('.field').forEach((field,index)=>{const label=field.querySelector('label')?.textContent?.trim()||'';const box=field.querySelector('.field-box');if(!box)return;let kind=label.includes('วันที่')||label.includes('กำหนดส่ง')?'date':label.includes('เวลา')?'time':'text';const input=document.createElement(kind==='text'&&index>3?'textarea':'input');if(input.tagName==='INPUT')input.type=kind;input.className='smartlife-real-input';input.style.cssText='width:100%;border:0;outline:0;background:transparent;color:inherit;font:inherit';input.value=kind==='date'?today:kind==='time'?'09:00':'';box.replaceChildren(input)});document.querySelector('.save-button')?.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();const fields=all('.smartlife-real-input').map(x=>x.value);localStorage.setItem('smartlife:activity-draft',JSON.stringify({type,title:fields[0]||'รายการใหม่',date:fields[1]||today,time:fields[2]||'09:00',location:fields[3]||'',note:fields[fields.length-1]||''}));send({type:'smartlife:navigate',href:'smartlife_save_'+type+'.html',pageKey})},true)}
if(pageKey.startsWith('user/smartlife_save_')&&['activity','task','appointment'].some(x=>pageKey.endsWith(x))){const draft=JSON.parse(localStorage.getItem('smartlife:activity-draft')||'{}');const boxes=all('.field-box');if(boxes[0]&&boxes[0].lastChild)boxes[0].lastChild.textContent=draft.title||'รายการใหม่';if(boxes[1])boxes[1].textContent=draft.date||'';if(boxes[2])boxes[2].textContent=draft.time||'';all('.color-swatch').forEach(s=>s.addEventListener('click',()=>{all('.color-swatch').forEach(x=>x.classList.remove('active'));s.classList.add('active')}));document.querySelector('.save-button')?.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();const start=new Date((draft.date||new Date().toISOString().slice(0,10))+'T'+(draft.time||'09:00')+':00');const end=new Date(start.getTime()+60*60*1000);const color=getComputedStyle(document.querySelector('.color-swatch.active')||document.querySelector('.color-swatch')).backgroundColor;const rgb=color.match(/\\d+/g)||[111,143,109];const hex='#'+rgb.slice(0,3).map(n=>Number(n).toString(16).padStart(2,'0')).join('');action('create-activity',{...draft,startAt:start.toISOString(),endAt:end.toISOString(),color:hex},'smartlife_calendar_day.html')},true)}};
window.addEventListener('message',event=>{let msg=event.data;if(typeof msg==='string'){try{msg=JSON.parse(msg)}catch{return}}if(msg?.type==='smartlife:data-result'&&msg.pageKey===pageKey)render(msg.data||{});if(msg?.type==='smartlife:data-action-result'&&msg.pageKey===pageKey){if(msg.ok){const target=window.__smartlifePendingTarget;if(target)send({type:'smartlife:navigate',href:target,pageKey});else{send({type:'smartlife:data-request',pageKey});alert(msg.data?.link?'สร้างลิงก์รีเซ็ตแล้ว: '+msg.data.link:'บันทึกข้อมูลสำเร็จ')}}else alert(msg.message||'บันทึกข้อมูลไม่สำเร็จ')}});
setupForms();setTimeout(()=>send({type:'smartlife:data-request',pageKey}),120);})();</script>`;
  return prepared.replace('</body>', `${script}</body>`);
}
